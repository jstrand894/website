```{=html}
<style type="text/css">
.grid-gallery {
  columns: 5 200px;
  column-gap: 0.5rem;
  width: 90%;
  margin: 0 auto;
}
.grid-item {
  width: 100%;
  border-radius: 5px;
  overflow: hidden;      /* Clip the image to the rounded corners */
  display: inline-block;
  will-change: transform; /* Hint to GPU to optimize transforms */
}

img.grid-item {
  width: 100%;
  transition: all 0.25s ease-in-out;
  display: block;
  backface-visibility: hidden; /* Helps with anti-aliasing during transform */
  transform-style: preserve-3d; /* Force 3D context */
}

img.grid-item:hover {
  transform: scale3d(1.025, 1.025, 1); /* 3D scaling avoids glitchy edges */
}
</style>
```

<%
function seededRandom(seed) {
  let x = Math.sin(seed++) * 10000;
  return x - Math.floor(x);
}

function shuffleArraySeeded(array, seed) {
  let arr = array.slice();
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(seededRandom(seed + i) * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr;
}

// Example: seed = 123
const shuffledItems = shuffleArraySeeded(items, 123);
%>

::: {.grid-gallery}
<% for (const item of shuffledItems) { %>
::: {}
![](<%= item.path %>){.grid-item .lightbox loading="lazy" group="quarto-grid-gallery"}
:::
<% } %>
:::
